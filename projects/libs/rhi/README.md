# Render Hardware Interface (RHI)

The RHI is an abstraction layer between the engine and graphics APIs such as Vulkan, Metal, or OpenGL.
The RHI provides a unified api for the renderer to talk to instead of calling Vulkan or OpenGL functions directly.
